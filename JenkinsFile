pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                checkout scm
            }
        }
        stage('build') {
            steps {
                sh 'docker build -t voidlight-back:' + env.BRANCH_NAME +  ' .'
            }
        }
        stage('tag') {
            steps {
                sh 'docker tag voidlight-back:' + env.BRANCH_NAME +  ' voidlight-back:' + env.BUILD_NUMBER
            }
        }
    }
}
