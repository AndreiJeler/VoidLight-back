pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                checkout scm
            }
        }
        stage('build') {
            steps {
                sh 'cp /home/polk15/voidlight/appsettings.json ./VoidLight.Web/'
                sh 'docker build -t voidlight-back:' + env.BRANCH_NAME +  ' .'
            }
        }
        stage('tag') {
            steps {
                sh 'docker tag voidlight-back:' + env.BRANCH_NAME +  ' voidlight-back:' + env.BUILD_NUMBER
            }
        }
        stage('deploy'){
            steps {
                sh 'docker run -dit --name voidlight-back -p 44324:44324 -p 5000:5000 -p 5001:5001 voidlight-back:' + env.BRANCH_NAME + ' '
            }
        }
    }
}
